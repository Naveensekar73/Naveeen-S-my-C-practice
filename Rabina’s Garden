Rabina’s Garden
#include<iostream>
using namespace std;
int main(){
    int a,b,c,d;
    cin>>a>>b>>c;
    //c=((a*b)-(2*a));
    //d=((a*b)-((a*b)-a));
    if(c%a==2 ||c%a==0 )
    {
        cout<<"It is a mango tree";
    }
    else
    {
        cout<<"It is not a mango tree";
    }
}
